<p style="color: green"><%= notice %></p>

<%= render @convenio %>

<div class="form-actions">
  <div class="row row-cols-auto">
    <div class="col">
      <%= link_to "Editar", edit_convenio_path(@convenio), class: "btn btn-primary" %> 
    </div>
    <div class="col">
      <%= button_to "Excluir", @convenio, form: { data: { turbo_confirm: "Você tem certeza que deseja excluir?" } }, method: :delete,  class: "btn btn-danger"%>
    </div>
    <div class="col">
      <%= link_to "Voltar", convenios_path, class: "btn btn-outline-secondary" %>
    </div>
  </div>
</div>

